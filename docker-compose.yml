services:
  app:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - f1data:/data
    restart: unless-stopped
    environment:
      - NODE_ENV=production
      - DATA_DIR=/data

volumes:
  f1data:
